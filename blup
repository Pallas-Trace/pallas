#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PORT="${BLUP_PORT:-5067}"

cd "$ROOT"

exec uv run bokeh serve \
    src/blup \
    --port "$PORT" \
    --show \
    --args "$@"
